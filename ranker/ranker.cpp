#include "ranker.h"

::vector<url_score> getHighest(::vector<Match*>* matches, ISR* queryRoot)
    {
    ISR** terms = queryRoot->GetTerms();
    for ( int i = 0; i < queryRoot->GetTermNum(); ++i ) 
        {

        }

    }